# 项目基线与长期准则

## 1. 项目定位

本仓库用于记录考研备考过程中的每日错题、重难点题目和阶段性测试材料。题目截图是原始资料，科目目录中的 Markdown 是对题目的轻量归纳，完整的演算与复盘保存在手写草稿中。

所有新建或维护的项目文档、自动化报告和测试题默认使用中文。

## 2. 当前基线

初始化时仓库已经包含以下资料：

- `过渡站/`：题目截图的临时放置区，目前可能为空。
- `数学/`：包含高等数学、线性代数及其题图和部分 Markdown 笔记。
- `408/`：包含数据结构、计算机组成原理、操作系统、计算机网络等方向的题图；某些题图可能暂时还没有对应 Markdown。
- `assets/`：各科目目录下的最终题图存放位置。

不得因为某张题图暂时没有 Markdown，或某篇 Markdown 暂时没有题图，就擅自删除、移动或重命名资料。

## 3. 文件与目录约定

### 3.1 题图

- 截图先进入 `过渡站/`，整理完成后放入对应科目的 `assets/`。
- 文件名应尽量表达“知识点-题型-方法-特殊提醒”，使用短横线分隔，保持中文可读和可检索。
- 不得使用会破坏跨平台路径的字符：`\ / : * ? " < > |`。
- 不覆盖同名文件。若确实是不同题目，应增加来源、序号或其他明确标识。
- 题图属于原始资料，未经用户明确要求，不得压缩、裁剪、改色、OCR 覆盖原图或删除。

### 3.2 Markdown 笔记

- 笔记按科目、知识模块和题型组织；现有结构优先于强行统一结构。
- 题目归纳至少应能说明：知识点、使用方法、特殊注意事项。
- 完整解题过程默认不由 AI 代写；若题图或 Markdown 无法确认某个推导，应标记为“待确认”。
- 新笔记引用题图时使用相对路径，例如：

  ```markdown
  ![题目说明](assets/题图文件名.png)
  ```

- 不为追求格式统一而批量改写已有笔记。发现历史绝对路径或其他兼容性问题时，先报告，再按用户确认的范围修复。

### 3.3 当前笔记体系

- 数学笔记放在具体科目目录中，以知识模块命名，例如 `数学/高等数学/多元微分.md`。正文通常使用二级标题划分知识模块，三级标题概括题型、知识点或主要方法，四级标题放置“题目”和“总结”。
- 同一题型下有多道题时，可使用“题目一”“题目二”等四级标题区分，每道题后分别保留“总结”位置，不把多道题的复盘混在一起。
- 408 笔记统一放在 `408/笔记/`，文件名采用“科目-章节-专题.md”的形式，例如 `计算机组成原理-存储系统-主存储器.md`。一篇笔记包含多个子专题时，使用三级标题划分子专题，再以四级标题放置“题目”和“解答”。
- 现有笔记中的“总结”“解答”“讲解”等复盘标题可以继续沿用；整理新题时优先跟随目标笔记已有用词，不为统一称呼而修改历史内容。
- 新增题图引用必须使用相对于笔记文件的路径。数学科目笔记通常使用 `assets/题图文件名.png`，`408/笔记/` 中的笔记使用 `../assets/题图文件名.png`。
- 归档题图时，先依据题目所属科目、知识模块、题型和主要方法判断已有笔记能否容纳；能够容纳时直接追加，不能容纳时再按现有命名形式新建专题笔记。
- 在用户准备亲自复盘的整理任务中，只插入题图并建立必要的题型标题，保留空白“总结”或“解答”区域，不代写结论、推导或答案。若原始题图本身包含解析、答案或图示，仍按原始资料完整保留，但不将其中内容转写为 AI 解答。
- 不因新增条目而批量调整旧标题层级、旧图片引用格式或旧文件名；历史内容只在用户明确指定范围后修复。

## 4. Git 基线

- Git 用于追踪每日新增题图、笔记修改和阶段性测试材料。
- 初始化提交应包含初始化时已经存在且属于本项目的资料，以及 `README.md` 和本文件。
- 每日整理完成后优先形成一次清晰提交，推荐提交信息为 `daily: YYYY-MM-DD`；周测可使用 `weekly: YYYY-Www`。
- 任何提交前都要检查 `git status`、`git diff` 和新增文件清单，避免误删、误移、重复加入或提交无关文件。
- 不使用破坏性 Git 操作覆盖用户资料，例如未经明确授权不得执行 `git reset --hard`、`git checkout --` 或批量删除。

## 5. 自动化准则

自动化已经实现，但必须保持只读、可预览、可重跑：扫描 Git 变更和 Markdown，生成报告，但不移动题图、不改写笔记、不删除文件、不自动提交。Windows 定时任务只有在 API 凭据验证通过后才应启用。

自动化读取范围限定为本仓库；手写草稿不在仓库中时，不得假设自己看过草稿内容。生成的统计和测试题应能回溯到具体的 Markdown 标题、题图文件或 Git 提交。

每日统计至少应检查：

- 新增或修改的 Markdown、题图和 `过渡站/` 文件；
- 各科目及知识点的题目数量；
- 题图链接是否存在、题图是否孤立、是否有疑似重复命名；
- 无法解析或需要用户确认的项目。

每周测试至少应做到：

- 题目来自已记录的笔记，变式题明确标注“变式”；
- 覆盖知识点和方法，而非只重复图片顺序；
- 每道题带来源引用，答案或核验依据与题目分离保存；
- 对原笔记信息不足、可能存在歧义的题目不擅自编造结论。

自动化生成物默认放在 `报告/每日/` 和 `报告/每周/`，保持未提交状态供用户复核。当前允许将题图发送到外部 AI 服务，但密钥只能通过环境变量或未纳入 Git 的安全配置提供，不得写入仓库；外部服务失败时必须在报告中说明，不得伪造归纳或测试题。

## 6. 维护优先级

发生冲突时，按以下顺序处理：

1. 保护原始题图和用户已有笔记；
2. 保证题图、Markdown 和 Git 记录之间可追溯；
3. 保持目录和命名可检索；
4. 再考虑格式统一、自动化便利和美化。

任何不确定的内容宁可列为待确认，也不要静默修改用户的学习资料。
