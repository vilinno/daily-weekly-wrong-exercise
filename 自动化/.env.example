# 复制为 自动化/.env 后填写；.env 已加入 .gitignore，不会被提交。
OPENAI_API_KEY=
# 兼容接口的根地址；脚本会拼接 /v1/chat/completions。
OPENAI_BASE_URL=https://ai.centos.hk
# 当前使用的模型。
OPENAI_MODEL=claude-opus-4-8
# 也可直接填写完整地址，优先级高于 OPENAI_BASE_URL。
# OPENAI_CHAT_COMPLETIONS_URL=https://ai.centos.hk/v1/chat/completions
