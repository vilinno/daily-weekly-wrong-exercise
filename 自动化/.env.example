# 复制为 自动化/.env 后填写；.env 已加入 .gitignore，不会被提交。
OPENAI_API_KEY=
# 可按账户可用模型修改；默认使用 config.json 中的 gpt-4.1-mini。
OPENAI_MODEL=gpt-4.1-mini
# 如使用兼容服务，可覆盖完整 Responses API 地址。
OPENAI_RESPONSES_URL=https://api.openai.com/v1/responses
