#### 题目

![[规定映射方式和写命中策略时的cache地址.png]]

#### 总结

主存块大小为32B，且按字节编址，所以块内地址大小用5位二进制位即可表示。

又cache数据区大小为32KB，主存块大小为32B，故一共1K个cache行，用10位二进制位表示。

又使用直接映射方式，所以标记位为32-5-10=17位。

而回写法需要一个脏位，加上cache的有效位，每个cache行的位数即$32*8+17+1+1=275$位。